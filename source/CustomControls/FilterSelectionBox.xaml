<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <ControlTemplate x:Key="FilterSelectionBoxTemplate" TargetType="{x:Type FilterSelectionBoxBase}">
        <Border BorderThickness="{DynamicResource ControlBorderThickness}"
                CornerRadius="{DynamicResource ControlCornerRadius}"
                Style="{DynamicResource HighlightBorder}"
                Background="{DynamicResource GlassControlBrushDark}"
                x:Name="Root">
            <Grid>
                <DockPanel>
                    <Viewbox Margin="0,1,10,0" DockPanel.Dock="Right" Width="10">
                        <TextBlock Text="&#xE96E;" Foreground="{DynamicResource TextSubtleBrush}" FontSize="18"
                                Style="{StaticResource IconFontStyle}"/>
                    </Viewbox>
                    <TextBlock x:Name="PART_TextFilterString"
                            Style="{StaticResource BaseTextBlockStyle}"
                            TextTrimming="CharacterEllipsis" HorizontalAlignment="Stretch"
                            VerticalAlignment="Center" DockPanel.Dock="Left"
                            Margin="8,0,25,0"/>
                </DockPanel>
                <ToggleButton x:Name="MainToggle"
                            BorderThickness="0" Focusable="False"
                            HorizontalContentAlignment="Stretch"
                            Background="Transparent">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border Background="Transparent">
                                            <ContentPresenter />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ToggleButton.Style>
                </ToggleButton>
                <TextBox x:Name="PART_TextFilterInput"
                        BorderThickness="0" Background="Transparent"
                        SelectionBrush="Transparent" CaretBrush="{DynamicResource TextBrush}"
                        VerticalAlignment="Center" Margin="0,-1,40,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button x:Name="PART_ButtonClearFilter" Foreground="{DynamicResource PromptBrush}"
                        Margin="0,1,20,0" FontSize="{DynamicResource FontSize}" Background="Transparent"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        BorderThickness="0" Padding="4,0,4,0"
                        ContentTemplate="{DynamicResource ClearTextIconTemplate}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, ElementName=Root}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Popup Placement="Bottom" Focusable="False" AllowsTransparency="True" Name="Popup"
                    IsOpen="{Binding IsChecked, ElementName=MainToggle, Mode=TwoWay, Delay=100}" StaysOpen="False"
                    MaxHeight="300" MinWidth="{Binding ActualWidth, ElementName=MainToggle}" VerticalOffset="5">
                    <Border Background="{DynamicResource SubtlePopUpBackgroundBrush}"
                            BorderThickness="{DynamicResource PopupBorderThickness}"
                            BorderBrush="{DynamicResource PopupBorderBrush}" CornerRadius="{DynamicResource RadiusCard}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <DockPanel Grid.Row="0" Margin="{DynamicResource Inset2}" Name="PART_ElemSearchHost">
                                <SearchBox DockPanel.Dock="Left" Margin="0" x:Name="PART_SearchBox"/>
                            </DockPanel>
                            <ItemsControl x:Name="PART_ItemsPanel" Grid.Row="1"/>
                        </Grid>
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding IsChecked, ElementName=MainToggle}" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource AccentHighlightBrush}" TargetName="Root"/>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="FilterSelectionBoxItemStyle" TargetType="{x:Type CheckBox}" BasedOn="{StaticResource ComboBoxListItemStyle}">
        <Setter Property="Padding" Value="8,0,8,8"/>
    </Style>

    <Style TargetType="{x:Type FilterEnumSelectionBox}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Template" Value="{DynamicResource FilterSelectionBoxTemplate}" />
    </Style>
    
    <Style TargetType="{x:Type FilterStringSelectionBox}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Template" Value="{DynamicResource FilterSelectionBoxTemplate}" />
    </Style>

    <Style TargetType="{x:Type FilterSelectionBox}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Template" Value="{DynamicResource FilterSelectionBoxTemplate}" />
    </Style>
</ResourceDictionary>