<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="NumericBoxTemplate" TargetType="{x:Type TextBox}">
        <Border x:Name="HoverBg"
                Padding="{DynamicResource Inset1}"
                CornerRadius="{DynamicResource ControlCornerRadius}"
                Background="{DynamicResource GlassControlBrushDark}"
                BorderBrush="{DynamicResource ControlIdleBorderBrush}"
                BorderThickness="{DynamicResource ControlBorderThickness}">
            <ScrollViewer x:Name="PART_ContentHost"
                        VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                        HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource ControlIdleBorderBrush}" TargetName="HoverBg"/>
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource AccentHighlightBrush}" TargetName="HoverBg"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type LongNumericBox}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Template" Value="{DynamicResource NumericBoxTemplate}"/>
    </Style>
    <Style TargetType="{x:Type UlongNumericBox}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Template" Value="{DynamicResource NumericBoxTemplate}"/>
    </Style>
    <Style TargetType="{x:Type NullIntNumericBox}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Template" Value="{DynamicResource NumericBoxTemplate}"/>
    </Style>
    <Style TargetType="{x:Type DoubleNumericBox}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Background" Value="{DynamicResource ControlIdleBackgroundBrush}"/>
        <Setter Property="Template" Value="{DynamicResource NumericBoxTemplate}"/>
    </Style>
</ResourceDictionary>