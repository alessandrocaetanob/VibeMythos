<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    mc:Ignorable="d">
    <Style TargetType="{x:Type NotificationPanel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="NotificationPanel">
                    <Border Background="{DynamicResource PopupBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource ContentBrushOverlay}"
                            CornerRadius="18"
                            d:DesignWidth="260" d:DesignHeight="400" Padding="{DynamicResource Inset3}"
                            d:DataContext="{x:Static DesignMainViewModel.DesignIntance}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                                <Button Name="PART_ButtonClose" DockPanel.Dock="Right" Height="30"
                                        Content="{DynamicResource LOCCloseLabel}"
                                        Background="{DynamicResource SecondaryButtonIdleBackgroundBrush}"
                                        FontSize="{DynamicResource FontSizeSmall}"
                                        Style="{DynamicResource SecondaryButtonMedium}"/>

                                <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center"
                                           Text="{DynamicResource LOCNotifications}"
                                           Style="{StaticResource PanelTitleTextBlockStyle}"/>
                            </DockPanel>

                            <ItemsControl Name="PART_ItemsMessages" Grid.Row="1">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource NotificationMessage}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                                <ItemsControl.Template>
                                    <ControlTemplate>
                                        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                                      VerticalScrollBarVisibility="Hidden">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </ControlTemplate>
                                </ItemsControl.Template>
                            </ItemsControl>

                            <Border Grid.Row="2">
                                <Button Name="PART_ButtonDismissAll"
                                        HorizontalAlignment="Stretch"
                                        Style="{StaticResource NotiButton}"
                                        Content="{DynamicResource LOCDismissAll}"/>
                            </Border>

                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>