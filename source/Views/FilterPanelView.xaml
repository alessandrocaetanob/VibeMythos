<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    mc:Ignorable="d">

    <Style x:Key="FilterPanelCheckBox" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
        <Setter Property="Margin" Value="0,2,0,8" />
    </Style>
    
    <Style x:Key="FilterPanelLabel" TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
        <Setter Property="Margin" Value="0,12,0,4" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize}"/>
        <Style.Triggers>
            <Trigger Property="Tag" Value="{StaticResource False}">
                <Setter Property="Foreground" Value="{DynamicResource SubtalBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <Style x:Key="FilterPanelFilterSearchBox" TargetType="SearchBox" BasedOn="{StaticResource {x:Type SearchBox}}">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Height" Value="30" />
    </Style>
    
    <Style x:Key="FilterPanelFilterSelectionBox" TargetType="FilterSelectionBox" BasedOn="{StaticResource {x:Type FilterSelectionBox}}">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Height" Value="30" />
    </Style>
    
    <Style x:Key="FilterPanelFilterEnumSelectionBox" TargetType="FilterEnumSelectionBox" BasedOn="{StaticResource {x:Type FilterEnumSelectionBox}}">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Height" Value="30" />
    </Style>
    
    <Style x:Key="FilterPanelFilterStringSelectionBox" TargetType="FilterStringSelectionBox" BasedOn="{StaticResource {x:Type FilterStringSelectionBox}}">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Height" Value="30" />
    </Style>

    <Style x:Key="FilterPresetButtonAction" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="FontFamily" Value="{DynamicResource IconFontFamily}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeLarge}"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Width" Value="28"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="DockPanel.Dock" Value="Right"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#1AFFFFFF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

<Style TargetType="{x:Type FilterPanel}">
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="{x:Type FilterPanel}">

                <Grid x:Name="FloatingWrapper"
                      Tag="{DynamicResource UseFloatingFilterPanel}">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Margin" Value="0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True">
                                    <Setter Property="Margin" Value="0,-8,0,16"/>
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True"/>
                                        <Condition Binding="{Settings ShowSidebar}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Margin" Value="16,-8,0,16"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="False"/>
                                        <Condition Binding="{Settings ShowSidebar}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Margin" Value="8,0,-8,0"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True"/>
                                        <Condition Binding="{Settings ShowSidebar}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Margin" Value="0,-8,16,16"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="False"/>
                                        <Condition Binding="{Settings ShowSidebar}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Margin" Value="-8,0,8,0"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <!-- Content + background in one layer -->
                    <Border x:Name="BorderContentHolder"
                            BorderBrush="{DynamicResource PanelSeparatorBrush}"
                            Tag="{DynamicResource UseFloatingFilterPanel}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{DynamicResource GlobalBackgroundBrush}"/>
                                <Setter Property="CornerRadius" Value="0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource GlassControlBrushDark}"/>
                                        <Setter Property="CornerRadius" Value="{DynamicResource GridCornerDetailsPanel}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <DockPanel Background="{TemplateBinding Background}" Margin="10" >
                            <DockPanel Margin="0,0,0,32" DockPanel.Dock="Top">
                                <Button Name="PART_ButtonClose" DockPanel.Dock="Right" Margin="-4"
                                        Content="&#xE8BB;" Width="35" Height="35"
                                        FontSize="{DynamicResource FontSizeSmall}" VerticalAlignment="Top"
                                        Style="{StaticResource GridCloseButton}"/>
                                <Button Name="PART_ButtonClear" DockPanel.Dock="Left" Height="30" Margin="-1"
                                        Content="{StaticResource LOCClearFilters}" VerticalAlignment="Top" HorizontalAlignment="Left"
                                        Style="{StaticResource DestructiveButton}"/>
                            </DockPanel>
                            <DockPanel Margin="0" DockPanel.Dock="Top">
                                <Button x:Name="PART_ButtonDeleteFilter"
                                        Content="&#xe107;" Style="{DynamicResource FilterPresetButtonAction}"/>
                                <Button x:Name="PART_ButtonRenameFilter"
                                        Content="&#xe104;" Style="{DynamicResource FilterPresetButtonAction}"/>
                                <Button x:Name="PART_ButtonSaveFilter"
                                        Content="&#xe105;" Style="{DynamicResource FilterPresetButtonAction}"/>
                                <ComboBox x:Name="PART_ComboFilterPresets" Margin="0,0,4,0"
                                          DockPanel.Dock="Left" VerticalAlignment="Center"/>
                            </DockPanel>
                            <Separator Height="1" DockPanel.Dock="Top"
                                       BorderBrush="#1AFFFFFF" Margin="0,16,0,16"
                                       Focusable="False" SnapsToDevicePixels="True"/>
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden"
                                          DockPanel.Dock="Top" Margin="0" >
                                <StackPanel x:Name="PART_PanelItemsHost" Margin="0,0,0,16"/>
                            </ScrollViewer>
                        </DockPanel>
                    </Border>

                    <!-- Border overlay -->
                    <Border x:Name="FloatingBorderOverlay"
                            IsHitTestVisible="False"
                            Tag="{DynamicResource UseFloatingFilterPanel}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="CornerRadius" Value="0"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True">
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="CornerRadius" Value="{DynamicResource GridCornerDetailsPanel}"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource ControlContrastBorderBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>

                </Grid>

            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>
</ResourceDictionary>